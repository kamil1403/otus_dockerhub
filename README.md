<p align="center">
  <img src="https://www.docker.com/wp-content/uploads/2022/03/vertical-logo-monochromatic.png" alt="Docker Banner" width="30%">
</p>

## ![Lesson](https://img.shields.io/badge/Lesson-otus__docker-0A84FF?style=for-the-badge&logo=docker&logoColor=white&labelColor=111827)![Author](https://img.shields.io/badge/Author-Kamil%20Ibragimov-10B981?style=for-the-badge&logo=github&logoColor=white&labelColor=111827)![Date](https://img.shields.io/badge/Date-14.12.2025-F59E0B?style=for-the-badge&logo=calendar&logoColor=white&labelColor=111827)

### 📌 Задание
1. Создать свой кастомный образ nginx на базе **alpine**.
2. Изменить дефолтную страницу nginx.
3. Ответить на вопросы:
    - В чем разница между образом и контейнером?
    - Можно ли в контейнере собрать ядро?
4. Запушить образ в Docker Hub.

### ✅ Результат
- [x] Образ собран на базе `nginx:alpine`.
- [x] Кастомная страница отображается (проверено через curl).
- [x] Образ загружен в Docker Hub: [kamilibg/otus-nginx-alpine](https://hub.docker.com/r/kamilibg/otus-nginx-alpine)

### 🧭 Оглавление
- [🧠 Теоретическая часть](#theory)
- [🛠 Техническая реализация](#tech)

---

<a id="theory"></a>
## 🧠 Теоретическая часть

#### 1. В чем разница между образом (Image) и контейнером (Container)?
* **Образ (Image)** — это **шаблон** (класс), неизменяемый файл, который содержит код приложения, библиотеки и зависимости. Это то, что мы скачиваем (`docker pull`) или собираем (`docker build`). Он хранится на диске.
* **Контейнер (Container)** — это **запущенный экземпляр** образа (объект). Это процесс в операционной системе, который работает в изолированном пространстве имен (namespaces) и имеет ограничения по ресурсам (cgroups). Контейнер имеет доступный для записи слой (read-write), который исчезает при удалении контейнера.

#### 2. Можно ли в контейнере собрать ядро Linux?
**Да, можно.**
Сборка ядра — это процесс компиляции исходного кода (C/C++), для которого требуются только компилятор (`gcc`), утилиты сборки (`make`, `binutils`) и исходный код ядра. Все эти инструменты можно установить внутри контейнера и успешно выполнить сборку.
**Важное примечание:** Запустить собранное ядро *внутри* того же контейнера нельзя, так как контейнеры используют ядро хостовой операционной системы. Результатом сборки будет бинарный файл ядра, который можно использовать на хосте или в виртуальной машине.

---

<a id="tech"></a>
## 🛠 Техническая реализация

### 1. Dockerfile
Используется легковесный базовый образ Alpine Linux.

```dockerfile
FROM nginx:alpine
COPY index.html /usr/share/nginx/html/index.html

2. Сборка и Запуск
Bash

# Сборка образа
docker build -t kamilibg/otus-nginx-alpine:v1 .

# Запуск контейнера в фоне на порту 80
docker run -d -p 80:80 --name my-alpine-site kamilibg/otus-nginx-alpine:v1
3. Проверка работы
Проверка локально через curl:

Bash

curl http://localhost
Вывод консоли:

HTML

<h1>Otus Docker HW: Hello from Alpine!</h1>
4. Публикация в Docker Hub
Bash

docker login -u kamilibg
docker push kamilibg/otus-nginx-alpine:v1
